{
    email dpfeiffer@gmail.com
}

llod.garage.golf {
    # Basic Auth using environment variable
    basicauth * {
        danny {env.HASHED_PASSWORD}
    }

    handle /api* {
        reverse_proxy backend:8000
    }
    handle /jobs* {
        reverse_proxy backend:8000
    }
    handle /accessories* {
        reverse_proxy backend:8000
    }
    handle {
        reverse_proxy frontend:3000
    }
}
